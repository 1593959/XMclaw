{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "XMclaw Daemon (HTTP + WebSocket)",
      "runtimeExecutable": "python",
      "runtimeArgs": ["-m", "uvicorn", "xmclaw.daemon.server:app", "--host", "127.0.0.1", "--port", "8080", "--reload"],
      "port": 8080
    }
  ]
}

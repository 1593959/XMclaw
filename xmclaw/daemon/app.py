"""FastAPI app — v2 daemon entry.

Phase 2: replaces the v1 ``xmclaw/daemon/server.py`` path-by-path. For now
this is an empty FastAPI instance so import paths can be checked.
"""
from __future__ import annotations

# from fastapi import FastAPI  # Phase 2 — re-enable when wiring routes
# app = FastAPI(title="XMclaw v2")
app = None  # placeholder until Phase 2

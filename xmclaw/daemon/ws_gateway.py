"""WebSocket gateway — Phase 2.

Anti-req #8: WS requires device-bound auth (see auth.py) before any message
flows. No unauthenticated connections touch the event bus.
"""
# Phase 2 — replace v1 xmclaw/gateway/ws.py with device-bound variant
